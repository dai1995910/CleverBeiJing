<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dwb.zhbj"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- 闪屏页面 -->
        <activity
            android:name="com.dwb.zhbj.activity.SplishActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 引导页面 -->
        <activity android:name="com.dwb.zhbj.activity.GuideActivity" >
        </activity>
        <!--  新闻详情页面 -->
        <activity android:name="com.dwb.zhbj.activity.NewsDetailActivity" />
        <!-- 主页面 -->
        <!-- 为了Indicator的样式所以设置一个Theme -->
        <activity
            android:name="com.dwb.zhbj.activity.MainActivity"
            android:theme="@style/Theme.PageIndicatorDefaults" >
        </activity>
    </application>

</manifest>